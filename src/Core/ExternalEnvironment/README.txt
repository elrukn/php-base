This is a base library of helpers to be used in application cores.
Contains references/implementations when Core needs to access some external service,
or perform an action that is essentially main application's concern.
